import React from 'react';
import './Search.css';

const Search = () => (
        <div className="search">
            <input type='text' className="searchfield" placeholder='Search by name..' />
        </div>
);

export default Search;